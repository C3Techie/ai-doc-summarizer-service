# Server Configuration
PORT=3000
NODE_ENV=development

# API Configuration
API_PREFIX=api
API_VERSION=v1

# MongoDB Configuration (Free tier option: MongoDB Atlas Free Cluster)
MONGODB_URI=mongodb://localhost:27017/aidocsummarizer

# OpenRouter Configuration (Free tier option: Use free or low-cost models like gpt-4o-mini)
OPENROUTER_API_KEY=your_openrouter_api_key

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# File Storage (Minio/S3 Mock - Using local storage for simplicity in this example)
# For production, replace this with actual S3/Minio config.
STORAGE_PATH=./uploads